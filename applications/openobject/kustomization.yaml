apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: openobject
    repo: https://raw.githubusercontent.com/maykinmedia/charts/gh-pages/
    version: "1.1.1"
    releaseName: openobject
    namespace: publicground
    valuesFile: helm-values/openobject.yaml

resources:
  - manifests
