profile: ambient

pilot:
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 512Mi
  # env: # uncomment when moving to ambient mode
  #   # for now required for Gateway-API TLSRoute support
  #   PILOT_ENABLE_ALPHA_GATEWAY_API: true
seccompProfile:
  type: RuntimeDefault

# --- Route images through ACR cache (added) ---
global:
  hub: acrcoreinfra720prd.azurecr.io/docker.io/istio
